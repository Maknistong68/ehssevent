# ─────────────────────────────────────────────────────────────────────────────
# Event Report — environment template
#
# The app runs on a mock layer by default, so NONE of these are required to run
# `npm run dev`. Copy this file to `.env.local` and fill values in only as you
# wire up real integrations. Validated by `src/lib/env.ts`.
#
# NEVER commit real secrets — `.env*.local` and `.env*.production` are gitignored.
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase (data + auth) ───────────────────────────────────────────────────
# Public project URL + anon key are safe for the browser bundle.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service-role key is SERVER-ONLY. Never expose it to the client.
SUPABASE_SERVICE_ROLE_KEY=

# ── App origin (absolute URLs, CSP connect-src, CSRF posture) ────────────────
# e.g. https://app.example.com
NEXT_PUBLIC_APP_ORIGIN=

# ── Image optimizer allowlist ────────────────────────────────────────────────
# Comma-separated hostnames the Next image optimizer may fetch from
# (e.g. your-project.supabase.co,cdn.example.com). Empty = no remote images.
NEXT_PUBLIC_IMAGE_DOMAINS=

# ── Transactional email provider ─────────────────────────────────────────────
EMAIL_API_KEY=
EMAIL_FROM=

# ── Error monitoring / observability (e.g. Sentry DSN) ───────────────────────
NEXT_PUBLIC_MONITORING_DSN=

# ── PDPL Data Protection Officer contact (shown in UI) ───────────────────────
NEXT_PUBLIC_DPO_EMAIL=
NEXT_PUBLIC_DPO_PHONE=
NEXT_PUBLIC_DPO_ADDRESS=

# ── Operating legal entity (shown in Terms / Privacy copy) ───────────────────
NEXT_PUBLIC_LEGAL_ENTITY_NAME=
NEXT_PUBLIC_LEGAL_EMAIL=
